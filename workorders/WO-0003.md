---
id: WO-0003
title: Add Task Queue (per-plan) + status workflow
priority: now
owner: molty
status: todo
worker: codex
repo: projects/marketing-tool
constraints:
  - No paid APIs
acceptance:
  - SQLite table for tasks (id, planId, title, type, status, createdAt, updatedAt, notes)
  - CRUD API: /api/tasks and /api/tasks/[id]
  - UI: show tasks on plan page, allow status changes
outputs:
  - artifacts/WO-0003/summary.md
---

## Why
A marketing engine without task execution tracking is half a product.
